first download kotlin lib and extract it here


to run:

cd to bin
run 
    kotlinc-native ../test.kt -o ../test
    to compile exe

navigate to kotlin folder
    open cmd
        run 
            test.exe