first download kotlin lib and extract it here


to run:

cd to `bin`
run 
    `kotlinc-native ../a.kt -o ../a`
    to compile exe

navigate to `kotlin` folder
    open cmd
        run 
            `a.exe`